fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
networkx==3.4.2
numpy==2.2.1
httpx==0.28.1
anthropic==0.42.0
pytest==8.3.4

# Optional: only needed if calibrating synthetic data against raw Kaggle downloads.
# Without credentials the data generator calibrates against each dataset's published
# summary statistics instead. Uncomment to enable raw-download calibration.
# kaggle==1.6.17
