# Track 02 — Evaluation Report

> Generated by `python -m src.qa.harness`. Scores the pipeline's independently-computed output against `data/heldout/` (which the pipeline path never reads). Honest metrics, including false-positive cost, per the track rubric.

- Corpus: **135** webhook events -> **129** disputes; **305** account risk profiles.
- Frozen evaluation clock: `1788436800` (2026-09-03T12:00:00Z), the corpus's own "now".

## 1. Category classification

| class | precision | recall | f1 | tp | fp | fn |
|---|---|---|---|---|---|---|
| `authorization` | 1.000 | 1.000 | 1.000 | 24 | 0 | 0 |
| `consumer_dispute` | 1.000 | 1.000 | 1.000 | 40 | 0 | 0 |
| `fraud` | 1.000 | 1.000 | 1.000 | 34 | 0 | 0 |
| `needs_manual_classification` | 1.000 | 1.000 | 1.000 | 11 | 0 | 0 |
| `processing_error` | 1.000 | 1.000 | 1.000 | 20 | 0 | 0 |
| **macro** | 1.000 | 1.000 | | | | |

Overall accuracy: **1.000**.

`needs_manual_classification` bucket (the one most chargeback bots skip): precision **1.000**, recall **1.000** (tp=11, fp=0, fn=0). An unmapped real network code is routed here explicitly, never guessed into the nearest category.

## 2. Risk flagging — planted mules vs. legitimately-bursty controls

Strict scoring (only the 3 planted mules count as positives, every other profiled account is a negative): precision **0.300**, recall **1.000**.

| account | held-out label | baseline_deviation | flagged HIGH? |
|---|---|---|---|
| `ACC_MULE_BRIDGE` | planted_mule | 14.3 | yes |
| `ACC_MULE_FANOUT` | planted_mule | 22.62 | yes |
| `ACC_MULE_PASSTHRU` | planted_mule | 20.52 | yes |
| `ACC_BURSTY_PAYDAY` | bursty_control | 0.0 | no |
| `ACC_BURSTY_SEASONAL` | bursty_control | 2.25 | no |

- Planted mules flagged HIGH: **3/3**. Bursty controls flagged HIGH: **0/2**.
- 10 accounts total sit at the HIGH band (`baseline_deviation >= 8.0`): 3 planted mules, 7 `normal`-labelled accounts (the documented cross-cluster / fan-out-recipient structural false positives — see `docs/failure-taxonomy.md`, risk-graph-service).

## 3. Routing — draft-for-submit vs. human review

Positive class = `assemble_clean` / `draft_for_submit`. Precision **0.936**, recall **0.868**, F1 **0.901**. Agreement with held-out dispositions: **0.899** (116/129).

| | held-out: assemble_clean | held-out: defer_to_human |
|---|---|---|
| pipeline: draft_for_submit | 59 | 4 |
| pipeline: human_review | 9 | 57 |

## 4. False-positive cost

**9** legitimate cases (held-out `assemble_clean`, not a hand-flagged borderline flip) were pushed to unnecessary human review.

- from a risk-flag false positive on a `normal` account: **3** (`disp_0073`, `disp_0271`, `disp_0337`)
- from an evidence-completeness / threshold miss: **6** (`disp_0031`, `disp_0133`, `disp_0172`, `disp_0181`, `disp_0196`, `disp_0325`)
- bursty-control disputes deferred because risk flagged that account: **0** (the false-positive target — 0 is the goal)

**Cost model.** 12 analyst-min/case to open+verify+dispatch a case ground truth says needed no human judgement; Rs 10/min fully-loaded.

=> **108 analyst-minutes** (~1.8 h) across a 129-dispute corpus; ~Rs 1,080 of avoidable analyst time.

**Bounded harm.** no frozen funds, no auto-contact, no auto-submit — the defense-only boundary caps the harm at analyst time + dispatch latency. The reverse error (4 held-out `defer_to_human` cases the pipeline drafted) costs ~0 operational — every one still lands in the draft_for_submit queue with dispatched=False, so a human reads it before anything is submitted; the miss is 'not escalated louder', not 'auto-sent'.

## Caveats

- planted-mule prevalence in the synthetic graph (~1% of accounts) is ~10x a real AML base rate — risk precision/recall is inflated vs a real deployment (inherited calibration note).
- held-out dispositions encode what the pipeline SHOULD do, not what would actually win a dispute — routing accuracy is agreement with designer intent, not a card-network outcome.
